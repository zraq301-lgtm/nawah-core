import React from 'react';
import { LayoutDashboard, Package, Activity, Settings, Cpu } from 'lucide-react';

export const DashboardShell = ({ children, tenantName }) => {
  return (
    <div className="flex h-screen bg-[#020617] text-slate-200 font-sans">
      {/* Sidebar */}
      <div className="w-64 border-r border-slate-800 p-6 flex flex-col bg-[#020617]">
        <div className="mb-10">
          <div className="flex items-center gap-2 mb-1">
             <Cpu className="text-indigo-500" size={24} />
             <h1 className="text-xl font-bold tracking-tight text-white">NAWAH AI-OS</h1>
          </div>
          <div className="flex items-center gap-2 px-1">
            <div className="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></div>
            <p className="text-[10px] text-slate-500 uppercase tracking-widest font-bold">Client: {tenantName}</p>
          </div>
        </div>
        
        <nav className="space-y-2 flex-1">
          <div className="flex items-center gap-3 p-3 rounded-xl bg-indigo-600/10 text-indigo-400 border border-indigo-500/20">
            <LayoutDashboard size={20} />
            <span className="text-sm font-semibold">Overview</span>
          </div>
          <div className="flex items-center gap-3 p-3 rounded-xl text-slate-400 hover:bg-slate-800/50 transition-all cursor-pointer">
            <Package size={20} />
            <span className="text-sm font-semibold">Inventory</span>
          </div>
        </nav>
      </div>

      {/* Content Area */}
      <main className="flex-1 overflow-auto bg-[#020617] p-8">
        <div className="max-w-7xl mx-auto">{children}</div>
      </main>
    </div>
  );
};
